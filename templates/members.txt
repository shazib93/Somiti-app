<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Somiti Members</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .card {
            transition: transform 0.3s;
        }
        .card:hover {
            transform: scale(1.05);
        }
    </style>
</head>
<body class="bg-light">
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="text-success">Probaho Bondhu Songhothon</h1>
            <a href="/logout" class="btn btn-danger">Logout</a>
        </div>
        <form method="GET" action="/members" class="mb-4">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Member khojo..." value="{{ search }}">
                <button type="submit" class="btn btn-success">Search</button>
            </div>
        </form>
        <div class="text-center mb-4">
            <a href="/add" class="btn btn-success">+ New Member Add Koro</a>
        </div>
        <div class="row">
            {% for member in members %}
            <div class="col-12 col-sm-6 col-md-4 mb-4">
                <div class="card text-center {% if member.amount >= 500 %}border-success{% else %}border-danger{% endif %}">
                    <div class="card-body">
                        <h5 class="card-title">{{ member.naam }}</h5>
                        <p class="card-text">Paid: <strong>{{ member.amount }} BDT</strong></p>
                        <p class="card-text">Month: {{ member.month }}</p>
                        <a href="/edit/{{ member.id }}" class="btn btn-warning btn-sm">Edit</a>
                        <a href="/delete/{{ member.id }}" class="btn btn-danger btn-sm">Delete</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>